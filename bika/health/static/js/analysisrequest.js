/** 
 * Javascript for AnalysisRequest view.
 */
(function( $ ) {
$(document).ready(function(){

    // Only if the view is the Analysis Request (edit) view
    if ($(".template-base_view #analysisrequest_edit_form").length > 0) {

        _p = jarn.i18n.MessageFactory('plone');
        _b = jarn.i18n.MessageFactory('bika');
        _ = jarn.i18n.MessageFactory('bika.health');

        // Add your code here
    }

});
}(jQuery));
